{
    "name": "enumtest",
    "private": false,
    "version": "0.0.1",
    "types": "./dist/index.d.ts",
    "type": "module",
    "scripts": {
        "dev": "vite --host",
        "build": "rimraf --glob build/**/* && tsc && vite build",
        "preview": "vite preview --port 8080"
    },
    "devDependencies": {
        "@types/jsdom": "^21.1.6",
        "@types/node": "^20.11.5",
        "copyfiles": "^2.4.1",
        "prettier": "^3.2.4",
        "rimraf": "^5.0.7",
        "ts-node": "^10.9.2",
        "typescript": "^5.5.0",
        "vite": "^6.0.1"
    }
}
